module.exports = {
    loginPage: 'https://login_page.com',        // Your login page URL
    usernameField: 'input[name="username"]',    // Specify the HTML field name for the username
    passwordField: 'input[name="password"]',    // Specify the HTML field name for the password
    usernameValue: 'your_username',             // Specify the username value
    passwordValue: 'your_password',             // Specify the password value
    submitButton: 'button[type="submit"]'       // Specify the type of the submit button
};
